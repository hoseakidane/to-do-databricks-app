bundle:
  name: databricks_app_bundle_testing

variables:
  prod_root_path:
    description: "Prod deployment path"
    default: "/Workspace/Users/hosea.kidane@databricks.com/.bundle/databricks_app_bundle_testing/prod"
  prod_user:
    description: "Prod SP id"
    default: "33f4858a-0464-4fb2-b1b0-20acb3dde95f"

resources:
  apps:
    todo_list_app_bundle:
      name: 'todo-list-app'
      source_code_path: .
      description: 'Todo list app for testing app bundle deployment'

targets:
  dev:
    mode: development
    default: true
    workspace:
      profile: e2-demo-west

  prod:
    mode: production
    workspace:
      host: https://fevm-serverless-4114v3.cloud.databricks.com/
      root_path: ${var.prod_root_path}
    permissions:
      - user_name: ${var.prod_user}
        level: CAN_MANAGE

